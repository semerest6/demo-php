# Project-specific AI rules

## Scope
This repository currently has no established product-specific AI rules beyond the shared core.

As the project gains real requirements, add only repository-specific rules here. Do not duplicate common rules from `.ai/core/`.

## Git
Use the existing branch when it is unambiguous. Do not perform destructive Git operations without explicit authorization.

## Project memory
`ai-docs/SESSION_LOG.md` is the project-local chronological memory and is never synchronized from ai-workflow-core.
